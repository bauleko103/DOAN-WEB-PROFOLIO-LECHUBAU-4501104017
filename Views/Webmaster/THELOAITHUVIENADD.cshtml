@model NATURALLIFE.Models.THELOAITHUVIEN

@{
    ViewBag.Title = "THELOAITHUVIENADD";
    Layout = "~/Views/Shared/_Webmaster.cshtml";
}

@using (Html.BeginForm("THELOAITHUVIENADD", "Webmaster", FormMethod.Post, new { enctype = "multipart/form-data" }))
{

    @Html.AntiForgeryToken()
    <section id="main-content">
        <section class="wrapper">
            <!-- row -->
            <div class="row mt">
                <div class="col-md-12">
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    @Html.HiddenFor(model => model.IDTHELOAI)

                    <div class="content-panel">

                        <table class="table table-striped table-advance table-hover">

                            <thead>
                                <tr>
                                    
                                    <th class="hidden-phone"><i class="fa "></i>@Html.DisplayNameFor(model => model.THELOAIANH)</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    
                                    <td>
                                        @Html.EditorFor(model => model.THELOAIANH, new { htmlAttributes = new { @class = "form-control" } })
                                        @Html.ValidationMessageFor(model => model.THELOAIANH, "", new { @class = "text-danger" })
                                    </td>
                                    <td>
                                    </td>
                            </tbody>
                        </table>


                    </div>
                    <!-- /content-panel -->
                    <button type="submit" class="btn btn-theme start" value="Create">
                        <i class="glyphicon glyphicon-save"></i>
                        <span>ADD</span>
                    </button>
                    <button type="button" class="btn btn-theme04 delete">
                        <i class="glyphicon glyphicon-arrow-left"></i>
                        <span>@Html.ActionLink("Back", "THELOAITHUVIENVIEW")</span>
                    </button>
                </div>

                <!-- /col-md-12 -->
            </div>
            <!-- /row -->
        </section>
    </section>
}